import 'package:flutter/material.dart';

class ApredictivoES extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Página de Análisis Predictivo - Entradas y Salidas'),
    );
  }
}