import 'package:flutter/material.dart';

class AvaloresatipicosD extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Página de Análisis de Valores Atípicos - Detección'),
    );
  }
}